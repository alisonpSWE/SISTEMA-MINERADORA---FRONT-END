import supabase from './supabase';

export const equipamentoService = {
  listar: async () => {
    // TODO: implement
    return { data: [] };
  }
};

export const funcionarioService = {
  listar: async () => {
    // TODO: implement
    return { data: [] };
  }
};
